<nav class="navbar navbar-expand-lg navbar-darker bg-secondary">
  <div class="container container-fluid">
    <a href="/">
      <img class="navbar-brand" src="/assets/img/logo.svg" alt="Logo" />
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Alterar visibilidade do menu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>
        <% categories.forEach((category) => { %>
        <li class="nav-item">
          <a class="nav-link" href="/categories/<%= category.slug %>"
            ><%= category.title %></a
          >
        </li>
        <% }); %>
      </ul>
    </div>
  </div>
</nav>
